import SwiftUI

// PassCheck — honest, on-device US passport-photo compliance checker.
// NOTE: authored on Windows without Xcode; first compiled/verified by CI (macOS runner).
@main
struct PassCheckApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
